{
  "name": "Your Spotify",
  "slug": "your-spotify",
  "categories": [
    13
  ],
  "date_created": "2026-08-03",
  "type": "ct",
  "updateable": true,
  "privileged": false,
  "architectures": [
    "amd64"
  ],
  "interface_port": 3000,
  "documentation": "https://github.com/Yooooomi/your_spotify#readme",
  "website": "https://github.com/Yooooomi/your_spotify",
  "repository": "https://github.com/Yooooomi/your_spotify",
  "logo": "https://cdn.jsdelivr.net/gh/selfhst/icons@main/webp/your-spotify.webp",
  "description": "Your Spotify records your Spotify listening history and turns it into statistics: top tracks, artists and albums over any period, listening habits by time of day, and long-term trends. Past history can be imported from a Spotify privacy data export.",
  "install_methods": [
    {
      "type": "default",
      "script": "ct/your-spotify.sh",
      "config_path": "/opt/your-spotify.env",
      "resources": {
        "cpu": 2,
        "ram": 3072,
        "hdd": 10,
        "os": "Debian",
        "version": "13"
      }
    }
  ],
  "default_credentials": {
    "username": null,
    "password": null
  },
  "app_vars": [
    {
      "name": "var_spotify_public",
      "label": "Spotify Client ID",
      "type": "text",
      "help": "From your app at developer.spotify.com/dashboard"
    },
    {
      "name": "var_spotify_secret",
      "label": "Spotify Client Secret",
      "type": "password",
      "secret": true
    },
    {
      "name": "var_api_endpoint",
      "label": "API Endpoint",
      "type": "text",
      "default": "",
      "help": "Public https URL of the API. Empty uses http://<container-ip>:8080, which Spotify rejects for login"
    },
    {
      "name": "var_client_endpoint",
      "label": "Client Endpoint",
      "type": "text",
      "default": "",
      "help": "Public https URL of the web UI. Empty uses http://<container-ip>:3000"
    }
  ],
  "notes": [
    {
      "text": "Spotify only accepts https redirect URIs (loopback excepted), so the Spotify login cannot be completed on the container's plain IP - it fails with 'redirect_uri: Insecure'. A TLS reverse proxy with a real domain is required for normal use.",
      "type": "warning"
    },
    {
      "text": "Create an app at https://developer.spotify.com/dashboard, put its client ID and secret into SPOTIFY_PUBLIC and SPOTIFY_SECRET in /opt/your-spotify.env, set API_ENDPOINT and CLIENT_ENDPOINT to your https URLs, and register <API_ENDPOINT>/oauth/spotify/callback in the dashboard. The API endpoint is baked into the built frontend; set both endpoints at install time, or change them in /opt/your-spotify.env and run 'update' inside the container, which re-applies them.",
      "type": "warning"
    },
    {
      "text": "Two services run here: your-spotify (API on 8080) and your-spotify-web (UI on 3000). Point the reverse proxy at both.",
      "type": "info"
    },
    {
      "text": "Listening history is stored in the local MongoDB database 'your_spotify'. Back that up rather than /opt.",
      "type": "info"
    }
  ]
}
